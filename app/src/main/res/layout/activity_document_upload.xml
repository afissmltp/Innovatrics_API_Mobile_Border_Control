<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="@android:color/white">

    <!-- Aperçu caméra -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#000" />

    <!-- Image/GIF loader -->
    <ImageView
        android:id="@+id/doc_upload"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginTop="8dp"
        android:scaleType="fitCenter"
        android:contentDescription="Chargement document"
        android:visibility="gone"/>

    <!-- Texte d’état -->
    <TextView
        android:id="@+id/upload_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Envoi du document..."
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:layout_marginTop="12dp"
        android:visibility="gone"/>

    <!-- Barre de progression -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_marginTop="12dp"
        style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Horizontal"
        android:indeterminate="true"
        android:visibility="gone"/>

    <!-- Bouton capture -->
    <Button
        android:id="@+id/captureButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Capturer"
        android:layout_marginTop="16dp"/>

</LinearLayout>
    <!--
    <?xml version="1.0" encoding="utf-8"?>
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        android:background="@android:color/white">

        &lt;!&ndash; Image du document &ndash;&gt;
        <ImageView
            android:id="@+id/doc_upload"
            android:layout_width="220dp"
            android:layout_height="360dp"
            android:layout_gravity="center_horizontal"
            android:contentDescription="Document à uploader"
            tools:ignore="UseAppTint"
            android:scaleType="fitCenter"/>

        &lt;!&ndash; Texte &ndash;&gt;
        <TextView
            android:id="@+id/upload_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Envoi du document en cours..."
            android:textSize="18sp"
            android:textColor="@android:color/black"
            android:layout_marginTop="24dp"
            android:gravity="center" />

        &lt;!&ndash; ProgressBar linéaire &ndash;&gt;
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginTop="16dp"
            android:indeterminate="true"
            android:indeterminateTint="@color/purple_app_bar"
            style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Horizontal" />

    </LinearLayout>
    -->