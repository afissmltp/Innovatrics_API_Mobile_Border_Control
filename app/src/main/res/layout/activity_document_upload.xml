<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Aperçu caméra qui prend tout l'écran -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"/>

    <!-- ImageView pour prévisualiser la photo capturée -->
    <ImageView
        android:id="@+id/previewImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone"
        android:contentDescription="Aperçu du document"/>

    <!-- Layout pour les boutons de confirmation -->
    <LinearLayout
        android:id="@+id/confirmationLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="16dp"
        android:visibility="gone">

        <Button
            android:id="@+id/retryButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="Réessayer"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/purple_app_bar"/>

        <Button
            android:id="@+id/confirmButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="OK"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/purple_app_bar"/>
    </LinearLayout>

    <!-- Image/GIF loader (initialement invisible) -->
    <ImageView
        android:id="@+id/doc_upload"
        android:layout_width="270dp"
        android:layout_height="270dp"
        android:layout_gravity="center"
        android:scaleType="fitCenter"
        android:contentDescription="Chargement document"
        android:visibility="gone"/>

    <!-- Texte d'état (initialement invisible) -->
    <TextView
        android:id="@+id/upload_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="100dp"
        android:text="Traitement du document en cours ..."
        android:textSize="20sp"
        android:textColor="@android:color/black"
        android:visibility="gone"/>

    <!-- Barre de progression (initialement invisible) -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="300dp"
        android:layout_height="8dp"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="70dp"
        android:indeterminateTint="@color/purple_app_bar"
        style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Horizontal"
        android:indeterminate="true"
        android:visibility="gone"/>

    <!-- Bouton de capture superposé -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/captureButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="30dp"
        android:src="@drawable/ic_camera"
        app:backgroundTint="@color/primary_color"/>

</FrameLayout>